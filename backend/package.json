{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "pm2": "mkdir -p logs && pm2 start server.js --name poem-api -i max --max-memory-restart 400M --log-date-format 'YYYY-MM-DD HH:mm:ss Z' --output ./logs/out.log --error ./logs/err.log --restart-delay 3000 --time && pm2 save && pm2 startup",
    "list": "pm2 list",
    "kill": "pm2 kill"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.10.0",
    "body-parser": "^2.2.0",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "mysql2": "^3.14.1",
    "nodemailer": "^7.0.5",
    "pm2": "^6.0.8"
  }
}
